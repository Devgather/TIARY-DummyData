runner:
  dummy:
    profile:
      enabled: true
      rows: 1000000
      batch-size: 10000

    account:
      enabled: true
      rows: 1000000
      batch-size: 10000

    verification:
      enabled: true
      rows: 1000000
      batch-size: 10000

    oauth:
      enabled: true
      rows-range-per-profile: 1-1
      batch-size: 10000

    til:
      enabled: true
      rows-range-per-profile: 0-100
      batch-size: 10000

    tag:
      enabled: true
      rows: 1000000
      batch-size: 10000

    til-tag:
      enabled: true
      rows-range-per-til: 0-10
      batch-size: 10000

    comment:
      enabled: true
      rows-range-per-til: 0-10
      batch-size: 10000