runner:
  dummy:
    profile:
      enabled: true
      rows: 1000000
      batch-size: 10000

    account:
      enabled: true
      rows: 1000000
      batch-size: 10000

    verification:
      enabled: true
      rows: 1000000
      batch-size: 10000

    oauth:
      enabled: true
      rows-range-per-profile: 1-1
      batch-size: 10000

    til:
      enabled: true
      rows-range-per-profile: 0-100
      batch-size: 10000